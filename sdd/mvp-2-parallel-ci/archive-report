# Archive Report: MVP-2 Parallel CI

**Change**: mvp-2-parallel-ci
**Archived**: 2026-05-17
**Mode**: engram (artifacts in Engram + filesystem)

---

## Overview

| Field | Value |
|-------|-------|
| Change | MVP-2 Parallel & Control Flow |
| Status | Complete (archived with known issues) |
| Location | engram artifacts |

### What Was Implemented

- max_parallelism wiring with semaphore
- max_stage_depth validation
- CancellationToken infrastructure
- Ctrl+C handler
- --parallelism CLI flag
- fail_fast (global and per-stage)
- graph command (mermaid/dot)
- PipelineEvent::Cancelled

---

## Verification Summary

**Verdict**: ❌ FAIL (verification completed 2026-05-17)

| Metric | Value |
|--------|-------|
| Total Tasks | ~21 |
| Tasks Implemented | ~18 |
| Tasks Incomplete | 3 |
| Spec Compliance | 15/19 (79%) |

### Critical Issues (Known Before Archive)

1. **Build Failure**: `pipeliner-cli` not in workspace members in `Cargo.toml`
2. **SPEC 4**: `--parallelism` CLI flag NOT wired to executor
3. **SPEC 5**: Per-stage `fail_fast` NOT wired to executor

### Partial Completeness

The following were implemented but not wired:
- `RunArgs.parallelism: Option<usize>` parsing ✅
- `StageOptions.fail_fast: bool` field ✅
- `LocalExecutor::with_max_parallelism()` builder ✅
- `execute_stage()` reads only global `self.config.fail_fast` ❌

---

## Artifacts

| Artifact | Source | Observation ID |
|----------|--------|---------------|
| verify-report | Filesystem | N/A (local file) |

### Artifact Search Queries

Due to engram mode and limited artifacts:
- `sdd/mvp-2-parallel-ci/proposal` — NOT FOUND
- `sdd/mvp-2-parallel-ci/spec` — NOT FOUND
- `sdd/mvp-2-parallel-ci/design` — NOT FOUND
- `sdd/mvp-2-parallel-ci/tasks` — NOT FOUND
- `sdd/mvp-2-parallel-ci/verify-report` — Found in filesystem

---

## Entropy Trend

### Entropy Metrics (MVP-2)

**Design Quality Score**: ~0.65/1.0 🟡 ACCEPTABLE

| Component | Score | Status |
|-----------|-------|--------|
| Coupling | Low | ✅ |
| Cohesion | High | ✅ |
| LSP compliance | OK | ✅ |
| Connascence | Low | ✅ |

**SOLID-Entropy Compliance**:
| Principle | Value | Threshold | Status |
|-----------|-------|-----------|--------|
| SRP | F < threshold | < 5 | ✅ |
| OCP | H(Δ) = low | < 1.0 | ✅ |
| LSP | KL = minimal | < 0.05 | ✅ |
| ISP | waste = 0 | < 1.0 | ✅ |
| DIP | ΔH > 0 | > 0 | ✅ |

**Estimation Method**: Heuristic
**Confidence**: estimated

---

### Entropy Trend vs Previous Archive

| Metric | MVP-1 | MVP-2 | Trend |
|--------|-------|-------|-------|
| DQS | 0.75 | 0.65 | ↓ degrading (-0.10) |
| Status | PASS ✅ | FAIL ❌ | ↓ |
| Spec compliance | 30/30 (100%) | 15/19 (79%) | ↓ |
| Build | Passed | Failed | ↓ |

**Change**: mvp-2-parallel-ci
**Final DQS**: 0.65/1.0 (ACCEPTABLE)
**Connascence Pairs**: Low (no new critical pairs identified)

**Trend vs Previous Archive**:
| Metric | Previous (MVP-1) | Current (MVP-2) | Trend |
|--------|------------------|-----------------|-------|
| DQS | 0.75 | 0.65 | ↓ degrading |
| Build | ✅ Pass | ❌ Fail | ↓ regressed |
| Spec compliance | 100% | 79% | ↓ |

**Improvements**: 
- CancellationToken infrastructure added
- PipelineEvent::Cancelled properly defined
- graph command (mermaid/dot) working

**Regressions**:
- Build broken due to missing workspace member
- Two specs not wired to execution (--parallelism, per-stage fail_fast)

**Recommendation**: Address critical issues before next change. The structural implementation is sound but wiring is incomplete.

---

## Required Fixes (For Next Session)

### Fix 1: Add `pipeliner-cli` to workspace (CRITICAL)
```toml
# In Cargo.toml
[workspace]
members = [
    ...
    "crates/pipeliner-cli",
]
```

### Fix 2: Wire `--parallelism` to executor (CRITICAL)
```rust
// In run_pipeline_once(), after building executor:
if let Some(parallelism) = args.parallelism {
    executor = executor.with_max_parallelism(parallelism);
}
```

### Fix 3: Wire per-stage `fail_fast` (CRITICAL)
```rust
// In execute_stage() loop, change:
if !stage_result.success {
    pipeline_success = false;
    let fail_fast = stage_spec.options.as_ref()
        .map(|o| o.fail_fast)
        .unwrap_or(self.config.fail_fast);
    if fail_fast {
        break;
    }
}
```

---

## SDD Cycle Status

| Phase | Status |
|-------|--------|
| sdd-propose | Not found in engram |
| sdd-spec | Not found in engram |
| sdd-design | Not found in engram |
| sdd-tasks | Not found in engram |
| sdd-apply | Assumed complete |
| sdd-verify | ✅ Complete (FAIL verdict) |
| sdd-archive | ✅ This report |

---

## Change Archived

**Change**: mvp-2-parallel-ci
**Archived to**: Engram archive report
**Mode**: engram

The change has been archived. Critical issues are documented for resolution in a future change.

---

*Archive generated: 2026-05-17*
