[package]
name = "pipeliner-protocol"
version = "0.1.0"
edition = "2024"
rust-version = "1.92.0"

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
thiserror = { workspace = true }
pipeliner-core = { workspace = true }

[dev-dependencies]
